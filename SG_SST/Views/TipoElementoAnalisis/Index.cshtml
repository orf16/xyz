@model IEnumerable<SG_SST.Models.Empresas.TipoElementoAnalisis>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoAnalisis.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion_Elemento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoAnalisis.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion_Elemento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PK_Tipo_Elemneto_Analisis }) |
            @Html.ActionLink("Details", "Details", new { id=item.PK_Tipo_Elemneto_Analisis }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PK_Tipo_Elemneto_Analisis })
        </td>
    </tr>
}

</table>
